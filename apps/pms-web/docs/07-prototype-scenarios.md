# 原型场景

内置 healthy、empty、loading、partial-data、network-error、degraded、runtime-stale、config-drift、catalog-breaking、worker-backlog、incident-active、pending-approval、read-only、permission-denied。场景通过 URL query 保持。
