{
  "name": "@sdar/pms-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run test",
    "build": "tsc -p tsconfig.json && node build-static.mjs"
  }
}
