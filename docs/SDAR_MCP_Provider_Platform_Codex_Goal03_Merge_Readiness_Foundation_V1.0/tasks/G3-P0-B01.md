# G3-P0-B01 — Lock Goal 03 baseline and isolate first failures

## Phase

`P0`

## Dependencies

None

## Objective

建立可复现的 Goal 03 基线，获取 `runtime-ci` 与 `runtime-compose` 的完整首错，禁止在本任务修改生产代码。

## Allowed Paths

```text
docs/review/GOAL03_BASELINE.md
.codex/goal-03/**
```

## Forbidden Paths / Actions

```text
Production source code
Tests
Dockerfile
.github/workflows/**
package.json
Migrations
Original Goal 2 task state/evidence
```

## Required Work

1. Fetch latest `origin/codex/goal-02-runtime-governance` and record actual SHA; do not assume the observed SHA is still current.
2. Record `origin/main`, PR #2 status, workflow run/job IDs, Node/pnpm/Docker/PostgreSQL versions.
3. Obtain full failed job logs using `gh run view --log-failed`, GitHub API, or equivalent authenticated method.
4. Record the **first causal failure**, not only the final exit code, for `pnpm verify:v2` and Docker Build.
5. Reproduce both failures locally when possible using clean install/build commands.
6. Create `docs/review/GOAL03_BASELINE.md` with exact commands, excerpts (no secrets), and proposed minimal path scope for B01/B02 fixes.
7. Confirm the original Goal 2 task state remains byte-for-byte unchanged.

## Mandatory Verification

```bash
git status --short
node --version
pnpm --version
docker --version
docker compose version
python3 .codex/goal-03-task-package/scripts/verify_goal2_state_unchanged.py .
python3 .codex/goal-03-task-package/scripts/taskctl.py status
```

## Acceptance Criteria

- Current Goal 2 remote SHA and main SHA are recorded.
- Both CI failures have an exact first failing command/error.
- No production source, test, workflow, Dockerfile or package script changed.
- A minimal allowed-path recommendation exists for the next two tasks.
- Goal 2 state is unchanged.

## Evidence

`.codex/goal-03/evidence/G3-P0-B01.json` with refs, run/job IDs, commands, exit codes, and sanitized failure summaries.

## Commit

```text
fix(goal03): G3-P0-B01 lock merge-readiness baseline
```

完成本任务后停止当前任务范围，使用 `taskctl.py pass`，提交并推送；不要提前执行后续任务。
