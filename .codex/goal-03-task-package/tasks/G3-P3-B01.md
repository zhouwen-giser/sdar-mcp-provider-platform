# G3-P3-B01 — Run Goal 03 acceptance and prepare handoff

## Phase

`P3`

## Dependencies

G3-P1-B03, G3-P2-B02

## Objective

执行完整 Goal 03 验收，推送分支，创建/更新 Goal 03 → Goal 2 的 Draft PR，并生成下一 Goal 可消费的交接文件。

## Allowed Paths

```text
.codex/goal-03/**
docs/review/GOAL03_FINAL_REPORT.md
docs/review/GOAL03_NEXT_GOAL_HANDOFF.md
No production code unless fixing a directly discovered Goal 03 regression and recorded in decisions.md
```

## Forbidden Paths / Actions

```text
New product features
PM2 Production Bridge
Worker Production Composition
Periodic scheduler
Release metadata/version/tag
Merging main
Modifying original Goal 2 task state
```

## Required Work

1. Rebase/merge latest `origin/codex/goal-02-runtime-governance`; resolve conflicts without dropping Goal 2 work.
2. Run all Goal 03 mandatory gates from a clean worktree.
3. Push the Goal 03 branch.
4. Create or update a Draft PR with base `codex/goal-02-runtime-governance`.
5. Wait for/inspect GitHub checks available to this branch; fix Goal 03 regressions only.
6. Generate final report with task/commit matrix, CI run IDs, exact deferred blockers and no false merge-ready claim.
7. Generate next-goal handoff for PM2 Production Bridge + Worker Production Composition + Periodic Scheduler.
8. Confirm Goal 2 original state unchanged and all 7 Goal 03 tasks PASSED.

## Mandatory Verification

```bash
pnpm install --frozen-lockfile
TEST_DATABASE_URL=<local-postgres> pnpm verify:v2
node scripts/verify-docker-workspace-manifests.mjs
docker compose config -q
docker compose build runtime adapter-typescript
docker compose up --detach --wait --wait-timeout 120
curl --fail --silent --show-error http://127.0.0.1:8080/health/ready
docker compose down --volumes
TEST_DATABASE_URL=<local-postgres> pnpm test:pms-api-production
TEST_DATABASE_URL=<local-postgres> pnpm test:pms-migrations
pnpm --filter @sdar/pms-worker test
pnpm exec vitest run packages/pms-application/test/runtime-reconcile.test.ts
pnpm typecheck
pnpm lint
pnpm format:check
git diff --check
python3 .codex/goal-03-task-package/scripts/verify_goal2_state_unchanged.py .
python3 .codex/goal-03-task-package/scripts/taskctl.py status
```

## Acceptance Criteria

- All seven tasks are PASSED.
- verify:v2, runtime-compose local path, PMS API production and focused Reconciler gates pass.
- Goal 03 branch is pushed and PR targets Goal 2, not main.
- Final report contains real command/CI evidence.
- Deferred PM2/Worker/Scheduler work is explicit.
- No release or merge-ready claim is made.
- Original Goal 2 task state is unchanged.

## Evidence

Final files: `.codex/goal-03/test-evidence.json`, `.codex/goal-03/handoff.json`, `docs/review/GOAL03_FINAL_REPORT.md`, `docs/review/GOAL03_NEXT_GOAL_HANDOFF.md`.

## Commit

```text
docs(goal03): G3-P3-B01 complete merge-readiness foundation handoff
```

完成本任务后停止当前任务范围，使用 `taskctl.py pass`，提交并推送；不要提前执行后续任务。
