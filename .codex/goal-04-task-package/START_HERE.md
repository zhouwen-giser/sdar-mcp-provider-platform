# Start Here

本包用于在 Goal 03 完成后执行最终的生产生命周期收口。

```bash
bash scripts/validate_package.sh
bash scripts/install_goal.sh /absolute/path/to/sdar-mcp-provider-platform
cd /absolute/path/to/sdar-mcp-provider-platform
cat .codex/goal-04-task-package/CODEX_MASTER_PROMPT.md
```

将 `CODEX_MASTER_PROMPT.md` 全文交给 Codex Goal Mode。预期首任务为 `G4-P0-B01`。
