# G1-P4-B06 — 实现 Audit 与 Job Lease 基础能力

- **阶段**：P4 PMS 核心与持久化
- **依赖**：G1-P4-B04
- **推荐提交**：`feat(pms): G1-P4-B06 add audit and job lease`

## 目标

为后续 Worker/长任务提供审计和安全领取。

## 允许修改范围

- `packages/pms-application/**`
- `packages/pms-persistence-postgres/**`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. audit context actor/correlation
2. append-only
3. lease claim/renew/release
4. 数据库时间
5. 并发测试

## 强制验证

```bash
pnpm test:pms
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] 两个 worker 不重复领取
- [ ] audit 不可更新删除
- [ ] 超时 lease 可恢复

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G1-P4-B06
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G1-P4-B06 --evidence '<command-or-evidence-path>'
```
