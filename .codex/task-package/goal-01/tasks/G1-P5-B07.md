# G1-P5-B07 — 实现 Runtime Config Watch 与 Ack

- **阶段**：P5 API 与配置中心
- **依赖**：G1-P5-B06
- **推荐提交**：`feat(runtime-config-api): G1-P5-B07 add watch and acknowledgements`

## 目标

提供 SSE revision hint 与结构化 Ack。

## 允许修改范围

- `apps/pms-api/**`
- `packages/configuration-center/**`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. SSE only sends revision/checksum
2. 断线可 latest 恢复
3. Ack statuses
4. 幂等 upsert
5. 实例身份校验

## 强制验证

```bash
pnpm test:pms-config-e2e
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] Watch 不发送完整 Secret config
- [ ] 重复 Ack 幂等
- [ ] 非法 revision 拒绝

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G1-P5-B07
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G1-P5-B07 --evidence '<command-or-evidence-path>'
```
