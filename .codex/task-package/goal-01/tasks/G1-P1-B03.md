# G1-P1-B03 — 物理拆分 Provider Migration 集合

- **阶段**：P1 Migration 隔离
- **依赖**：G1-P1-B02
- **推荐提交**：`refactor(migrations): G1-P1-B03 isolate provider migrations`

## 目标

把 UGV/NPC SQL 移至各自 Provider 目录，保持内容。

## 允许修改范围

- `migrations/providers/**`
- `migrations/migration-source-map.json`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. 创建 ugv/npc-tank 目录
2. 移动 024/025 并可重编号显示但保留 legacy id 元数据
3. 更新 source map
4. 不改变 SQL

## 强制验证

```bash
python3 .codex/task-package/scripts/verify_migration_source_map.py
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] Provider SQL hash 不变
- [ ] Runtime 目录无 Provider 表 SQL
- [ ] Provider 目录互相隔离

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G1-P1-B03
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G1-P1-B03 --evidence '<command-or-evidence-path>'
```
