# G1-P1-B08 — 完成 Migration 兼容与阶段门禁

- **阶段**：P1 Migration 隔离
- **依赖**：G1-P1-B07
- **推荐提交**：`docs(migrations): G1-P1-B08 close migration isolation phase`

## 目标

清理旧根扫描逻辑，提供升级说明和 P1 报告。

## 允许修改范围

- `docs/database/MIGRATION_SET_UPGRADE.md`
- `.codex/reports/phase-P1.md`
- `scripts/**`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. 搜索根 migrations 扫描引用
2. 删除或显式拒绝隐式扫描
3. 记录旧路径映射与回滚原则
4. 运行阶段回归

## 强制验证

```bash
rg "migrations" apps packages | head -200
```
```bash
pnpm test:migration-isolation
```
```bash
pnpm build
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] 无隐式根扫描
- [ ] P1 全部 PASSED
- [ ] 升级文档可执行

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G1-P1-B08
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G1-P1-B08 --evidence '<command-or-evidence-path>'
```
