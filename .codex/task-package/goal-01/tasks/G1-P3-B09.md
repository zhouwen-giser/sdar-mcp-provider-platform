# G1-P3-B09 — 生成 JSON Schema 与 PMS 表单元数据

- **阶段**：P3 共享配置契约
- **依赖**：G1-P3-B03、G1-P3-B04、G1-P3-B05、G1-P3-B06、G1-P3-B07、G1-P3-B08
- **推荐提交**：`feat(config-contract): G1-P3-B09 generate schemas and form metadata`

## 目标

从同一 ConfigurationDefinition 生成 JSON Schema、默认值和 UI metadata。

## 允许修改范围

- `packages/runtime-configuration-contract/src/generator/**`
- `schemas/config/**`
- `scripts/**`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. 实现确定性 generator
2. Secret 字段输出 writeOnly/secretRef metadata
3. 生成结果纳入版本控制
4. 加入 drift check

## 强制验证

```bash
pnpm config:schema:generate
```
```bash
pnpm config:schema:check
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] 重复生成无 diff
- [ ] Schema 与 Zod 同源
- [ ] UI metadata 有 apply mode/description

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G1-P3-B09
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G1-P3-B09 --evidence '<command-or-evidence-path>'
```
