# G1-P0-B02 — 建立 Git 基线与来源追踪

- **阶段**：P0 基线锁定
- **依赖**：G1-P0-B01
- **推荐提交**：`chore(repo): G1-P0-B02 establish source lock`

## 目标

确保当前仓库存在不可变初始导入提交、来源锁和工作分支。

## 允许修改范围

- `.git/**`
- `docs/baseline/SOURCE_LOCK.json`
- `.codex/**`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. 确认 main 基线提交存在
2. 记录初始 commit、ZIP SHA、导入时间、顶层目录
3. 确认当前分支为 codex/goal-01-platform-foundation
4. 禁止修改历史

## 强制验证

```bash
git status --short
```
```bash
git log --oneline -3
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] SOURCE_LOCK.json 完整
- [ ] 工作树只有本任务预期变更
- [ ] 分支和初始提交可追踪

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G1-P0-B02
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G1-P0-B02 --evidence '<command-or-evidence-path>'
```
