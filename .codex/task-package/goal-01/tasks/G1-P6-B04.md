# G1-P6-B04 — 完成 Goal 1 验收与 Handoff

- **阶段**：P6 Runtime Config Client
- **依赖**：G1-P6-B03
- **推荐提交**：`chore(delivery): G1-P6-B04 complete goal 01 handoff`

## 目标

运行 Goal 1 全量门禁，生成 50/50 状态、交付报告和 Goal 2 Handoff。

## 允许修改范围

- `.codex/handoff/**`
- `.codex/reports/goal-01-final.md`
- `docs/**`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. 运行 acceptance matrix
2. 记录外部环境缺口但不得伪造通过
3. 生成 goal1-handoff.json
4. 运行 verify_goal1_handoff.py
5. 确保工作树干净

## 强制验证

```bash
pnpm build
```
```bash
pnpm test:migration-isolation
```
```bash
pnpm test:provider-packages
```
```bash
pnpm test:config-compat
```
```bash
pnpm test:pms-config-e2e
```
```bash
python3 .codex/task-package/scripts/verify_goal1_handoff.py --repo .
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] 50 tasks PASSED
- [ ] Handoff Schema 通过
- [ ] Goal 2 所需包/API/表清单完整

## 完成证据

- `.codex/handoff/goal1-handoff.json`
- `.codex/handoff/goal1-delivery-report.md`

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G1-P6-B04
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G1-P6-B04 --evidence '<command-or-evidence-path>'
```
