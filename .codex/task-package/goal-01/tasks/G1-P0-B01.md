# G1-P0-B01 — 校验离线交付来源并生成基线清单

- **阶段**：P0 基线锁定
- **依赖**：无（Goal/阶段入口任务）
- **推荐提交**：`docs(baseline): G1-P0-B01 lock offline delivery baseline`

## 目标

验证源码 ZIP、交付 Manifest、SHA256SUMS、应用/包/Migration/测试数量，形成机器可读盘点。

## 允许修改范围

- `docs/baseline/**`
- `.codex/**`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. 运行包级 SHA 校验
2. 读取 WORK_DELIVERY_MANIFEST.json 和 SHA256SUMS.txt
3. 生成 BASELINE_INVENTORY.json 与 BASELINE_REPORT.md
4. 记录环境可用性，不修改源码

## 强制验证

```bash
bash .codex/task-package/scripts/verify_source_baseline.sh
```
```bash
python3 .codex/task-package/scripts/taskctl.py status
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] source SHA 与任务包固定值一致
- [ ] 盘点文件包含 apps/packages/migrations/tests/versions
- [ ] 无产品代码变更

## 完成证据

- `docs/baseline/BASELINE_INVENTORY.json`
- `docs/baseline/BASELINE_REPORT.md`

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G1-P0-B01
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G1-P0-B01 --evidence '<command-or-evidence-path>'
```
