# G1-P2-B05 — 实现 Provider Package Loader 与 Registry

- **阶段**：P2 Provider Package
- **依赖**：G1-P2-B02、G1-P2-B03、G1-P2-B04
- **推荐提交**：`feat(provider-package): G1-P2-B05 implement package registry`

## 目标

实现确定性加载、校验、按 ID/类型查询。

## 允许修改范围

- `packages/provider-package-registry/**`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. 只扫描受控 provider-packages 根
2. 拒绝重复 packageId+version
3. 稳定排序
4. 暴露 list/get/validate APIs

## 强制验证

```bash
pnpm --filter @sdar/provider-package-registry test
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] 三个内置包可加载
- [ ] 重复/损坏包失败
- [ ] 结果顺序稳定

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G1-P2-B05
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G1-P2-B05 --evidence '<command-or-evidence-path>'
```
