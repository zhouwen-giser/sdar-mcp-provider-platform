# G1-P2-B06 — 隔离 Mock Fixtures 与资格状态

- **阶段**：P2 Provider Package
- **依赖**：G1-P2-B05
- **推荐提交**：`fix(provider-package): G1-P2-B06 enforce qualification boundaries`

## 目标

确保 mock 应用只作为测试资产，资格模型不夸大。

## 允许修改范围

- `packages/provider-package-registry/**`
- `docs/providers/**`
- `tests/**`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. 定义 qualification 枚举
2. 测试 mock-* 不进入生产列表
3. 增加 qualification projection
4. 文档解释 component/real/system

## 强制验证

```bash
pnpm test:provider-packages
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] 生产列表无 mock package
- [ ] 资格状态可审计
- [ ] API 不输出 Certified 误导字段

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G1-P2-B06
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G1-P2-B06 --evidence '<command-or-evidence-path>'
```
