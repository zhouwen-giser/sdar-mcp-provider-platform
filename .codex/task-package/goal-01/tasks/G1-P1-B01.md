# G1-P1-B01 — 建立 Migration 所有权与路径映射

- **阶段**：P1 Migration 隔离
- **依赖**：G1-P0-B06
- **推荐提交**：`docs(migrations): G1-P1-B01 map migration ownership`

## 目标

为原 001～025 Migration 建立 owner、旧路径、新路径、SHA 映射。

## 允许修改范围

- `migrations/migration-source-map.json`
- `docs/database/**`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. 计算每个 SQL SHA-256
2. 001～023 owner=runtime
3. 024 owner=provider:ugv
4. 025 owner=provider:npc-tank
5. 定义 PMS 新 Migration 命名规则

## 强制验证

```bash
python3 .codex/task-package/scripts/verify_migration_source_map.py
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] 26 个根 SQL 全部映射
- [ ] 原内容 hash 固定
- [ ] 没有重复或缺失 sequence

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G1-P1-B01
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G1-P1-B01 --evidence '<command-or-evidence-path>'
```
