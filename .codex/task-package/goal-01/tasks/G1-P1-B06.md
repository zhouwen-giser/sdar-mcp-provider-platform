# G1-P1-B06 — 改造 UGV 与 NPC Provider Migration Runner

- **阶段**：P1 Migration 隔离
- **依赖**：G1-P1-B04
- **推荐提交**：`refactor(provider-db): G1-P1-B06 bind provider migration sets`

## 目标

让两个 Adapter migrate 入口只使用自己的 migration set。

## 允许修改范围

- `apps/ugv-provider-adapter/src/migrate.ts`
- `apps/npc-tank-provider-adapter/src/migrate.ts`
- `tests/**`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. UGV 绑定 provider:ugv
2. NPC 绑定 provider:npc-tank
3. 验证未知/交叉 set 不可达
4. 保持现有命令入口

## 强制验证

```bash
pnpm typecheck
```
```bash
pnpm test:unit
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] UGV/NPC 不扫描 Runtime SQL
- [ ] 两个 Provider 互不串表
- [ ] 入口行为有测试

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G1-P1-B06
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G1-P1-B06 --evidence '<command-or-evidence-path>'
```
