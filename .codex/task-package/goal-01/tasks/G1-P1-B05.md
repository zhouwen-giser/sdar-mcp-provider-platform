# G1-P1-B05 — 改造 Runtime Migration Runner

- **阶段**：P1 Migration 隔离
- **依赖**：G1-P1-B04
- **推荐提交**：`refactor(runtime-db): G1-P1-B05 bind runtime migration set`

## 目标

让现有 Runtime migrate/startup 只使用 runtime set，并保持 CLI 兼容。

## 允许修改范围

- `apps/runtime/src/migrate.ts`
- `packages/persistence-postgres/**`
- `tests/**`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. 定位原 runMigrations
2. 注入显式 runtime set
3. 保持 advisory lock/version/checksum
4. 保留原 db:migrate 命令

## 强制验证

```bash
pnpm db:migrate --help || true
```
```bash
pnpm --filter @sdar/persistence-postgres test
```
```bash
pnpm typecheck
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] Runtime 不再扫描 Provider SQL
- [ ] 现有迁移顺序保持
- [ ] 启动调用点兼容

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G1-P1-B05
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G1-P1-B05 --evidence '<command-or-evidence-path>'
```
