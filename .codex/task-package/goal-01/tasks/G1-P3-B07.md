# G1-P3-B07 — 抽取 NPC Tank Provider 配置契约

- **阶段**：P3 共享配置契约
- **依赖**：G1-P3-B02
- **推荐提交**：`refactor(npc-config): G1-P3-B07 share NPC configuration contract`

## 目标

统一 NPC 配置及条件能力。

## 允许修改范围

- `packages/runtime-configuration-contract/src/providers/npc-tank*`
- `apps/npc-tank-provider-adapter/src/config.ts`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. 映射 MQTT/Device MCP/TLS/Capability
2. 保持条件默认值
3. 旧 API 兼容层
4. 测试能力组合

## 强制验证

```bash
pnpm --filter @sdar/npc-tank-provider-adapter test
```
```bash
pnpm --filter @sdar/runtime-configuration-contract test
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] 旧配置行为一致
- [ ] 非法能力组合失败
- [ ] 无 PMS 启动依赖

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G1-P3-B07
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G1-P3-B07 --evidence '<command-or-evidence-path>'
```
