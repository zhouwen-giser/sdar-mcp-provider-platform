# G1-P2-B03 — 创建 NPC Tank Provider Package 描述

- **阶段**：P2 Provider Package
- **依赖**：G1-P2-B01
- **推荐提交**：`feat(provider-package): G1-P2-B03 add NPC tank package`

## 目标

标准化 NPC Tank Provider。

## 允许修改范围

- `provider-packages/npc-tank/**`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. 提取入口/配置/Migration
2. 标记 Mock Level 1 component 状态
3. realResourceStatus=pending
4. 记录 capability 条件

## 强制验证

```bash
pnpm test:provider-packages
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] Schema 通过
- [ ] 资格诚实
- [ ] migration set 正确

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G1-P2-B03
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G1-P2-B03 --evidence '<command-or-evidence-path>'
```
