# G1-P1-B02 — 物理拆分 Runtime Migration 集合

- **阶段**：P1 Migration 隔离
- **依赖**：G1-P1-B01
- **推荐提交**：`refactor(migrations): G1-P1-B02 isolate runtime migration files`

## 目标

把原 Runtime SQL 移至 migrations/runtime，保持字节内容与文件名。

## 允许修改范围

- `migrations/runtime/**`
- `migrations/*.sql`
- `migrations/migration-source-map.json`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. 移动 001～023 所有文件（含重复 014 文件名排序规则）
2. 更新 source map 新路径
3. 不修改 SQL 内容
4. 保留兼容说明

## 强制验证

```bash
python3 .codex/task-package/scripts/verify_migration_source_map.py
```
```bash
git diff --check
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] Runtime SQL hash 与原值相同
- [ ] 根目录不再被 Runtime 扫描
- [ ] 所有文件可定位

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G1-P1-B02
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G1-P1-B02 --evidence '<command-or-evidence-path>'
```
