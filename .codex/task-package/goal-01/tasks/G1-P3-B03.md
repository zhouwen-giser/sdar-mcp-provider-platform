# G1-P3-B03 — 抽取 Runtime 身份与数据库配置契约

- **阶段**：P3 共享配置契约
- **依赖**：G1-P3-B02
- **推荐提交**：`refactor(runtime-config): G1-P3-B03 extract identity and database contract`

## 目标

抽取 PROVIDER_ID、实例身份、数据库与 Adapter bootstrap 配置，不改变现有默认。

## 允许修改范围

- `packages/runtime-configuration-contract/src/runtime/**`
- `apps/runtime/src/config.ts`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. 身份 immutable
2. DB/Adapter/port restart_required
3. Secret 改为支持 *_FILE 但保持旧 env 兼容
4. 增加冲突优先级测试

## 强制验证

```bash
pnpm --filter @sdar/runtime-configuration-contract test
```
```bash
pnpm --filter @sdar/runtime test
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] 旧有效 env 仍通过
- [ ] 文件 Secret 有明确定义
- [ ] 身份不可被远程配置覆盖

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G1-P3-B03
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G1-P3-B03 --evidence '<command-or-evidence-path>'
```
