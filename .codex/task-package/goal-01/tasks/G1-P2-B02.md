# G1-P2-B02 — 创建 UGV Provider Package 描述

- **阶段**：P2 Provider Package
- **依赖**：G1-P2-B01
- **推荐提交**：`feat(provider-package): G1-P2-B02 add UGV package`

## 目标

把 UGV 入口、配置、Migration、兼容版本和资格状态标准化。

## 允许修改范围

- `provider-packages/ugv/provider-package.json`
- `provider-packages/ugv/README.md`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. 从源码/报告提取事实
2. hostingModes 含 vendor/platform
3. migrationSet=provider:ugv
4. 真实资格保持 pending
5. 引用已有验证 evidence

## 强制验证

```bash
pnpm test:provider-packages
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] 包通过 Schema
- [ ] 不把 Mock 结果声明为真实资格
- [ ] 入口和 package 名可解析

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G1-P2-B02
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G1-P2-B02 --evidence '<command-or-evidence-path>'
```
