# G1-P5-B01 — 建立 PMS API 应用骨架与错误合同

- **阶段**：P5 API 与配置中心
- **依赖**：G1-P4-B07
- **推荐提交**：`feat(pms-api): G1-P5-B01 scaffold control-plane API`

## 目标

新增版本化 API、健康、OpenAPI、统一错误与审计上下文。

## 允许修改范围

- `apps/pms-api/**`
- `packages/pms-application/**`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. 选择仓库既有 HTTP 风格或 Fastify
2. /health/live ready
3. /api/v1
4. requestId/correlationId
5. error envelope

## 强制验证

```bash
pnpm --filter @sdar/pms-api test
```
```bash
pnpm typecheck
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] API 可启动
- [ ] 错误不泄漏 Secret/SQL
- [ ] OpenAPI 可生成

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G1-P5-B01
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G1-P5-B01 --evidence '<command-or-evidence-path>'
```
