# G1-P5-B06 — 实现 Runtime Config Latest 与 ETag

- **阶段**：P5 API 与配置中心
- **依赖**：G1-P5-B05
- **推荐提交**：`feat(runtime-config-api): G1-P5-B06 expose latest config`

## 目标

提供按 deployment/instance 解析的 Published Effective Config。

## 允许修改范围

- `apps/pms-api/**`
- `packages/configuration-center/**`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. 独立 runtime client auth port
2. GET latest
3. ETag=checksum
4. If-None-Match 304
5. SecretRef projection

## 强制验证

```bash
pnpm test:pms-config-e2e
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] 304 正确
- [ ] 无明文 Secret
- [ ] 身份字段不可由层级覆盖

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G1-P5-B06
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G1-P5-B06 --evidence '<command-or-evidence-path>'
```
