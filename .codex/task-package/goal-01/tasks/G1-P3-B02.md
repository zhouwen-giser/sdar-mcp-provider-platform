# G1-P3-B02 — 定义共享 ConfigurationDefinition 基础类型

- **阶段**：P3 共享配置契约
- **依赖**：G1-P3-B01
- **推荐提交**：`feat(config-contract): G1-P3-B02 define configuration metadata`

## 目标

建立 schema/defaults/secretPaths/applyMode/overridePolicy 模型。

## 允许修改范围

- `packages/runtime-configuration-contract/**`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. 定义字段级 metadata
2. 支持目标类型与继承
3. canonical serialization
4. 错误 code 稳定

## 强制验证

```bash
pnpm --filter @sdar/runtime-configuration-contract test
```
```bash
pnpm typecheck
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] 类型不依赖 PMS/Runtime infra
- [ ] 非法 apply mode 失败
- [ ] canonical 输出稳定

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G1-P3-B02
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G1-P3-B02 --evidence '<command-or-evidence-path>'
```
