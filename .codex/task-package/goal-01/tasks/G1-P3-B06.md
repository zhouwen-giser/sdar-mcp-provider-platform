# G1-P3-B06 — 抽取 UGV Provider 配置契约

- **阶段**：P3 共享配置契约
- **依赖**：G1-P3-B02
- **推荐提交**：`refactor(ugv-config): G1-P3-B06 share UGV configuration contract`

## 目标

把 UGV config.ts 迁移到共享定义并保持 Adapter API。

## 允许修改范围

- `packages/runtime-configuration-contract/src/providers/ugv*`
- `apps/ugv-provider-adapter/src/config.ts`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. 映射 MQTT/Device MCP/TLS/Freshness/Safety
2. 标记 Secret
3. 保留现有 parseConfig export 兼容层
4. 增加旧环境 fixture

## 强制验证

```bash
pnpm --filter @sdar/ugv-provider-adapter test
```
```bash
pnpm --filter @sdar/runtime-configuration-contract test
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] 旧 UGV 配置行为一致
- [ ] Secret/生产校验一致
- [ ] Adapter 不依赖 PMS

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G1-P3-B06
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G1-P3-B06 --evidence '<command-or-evidence-path>'
```
