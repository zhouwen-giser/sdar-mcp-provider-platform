# G1-P5-B02 — 实现 Provider Package 查询 API

- **阶段**：P5 API 与配置中心
- **依赖**：G1-P5-B01
- **推荐提交**：`feat(pms-api): G1-P5-B02 expose provider packages`

## 目标

提供内置包列表、详情与资格投影。

## 允许修改范围

- `apps/pms-api/**`
- `packages/pms-application/**`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. GET list/detail
2. 稳定排序/filter
3. 不返回内部文件路径/敏感 evidence
4. OpenAPI tests

## 强制验证

```bash
pnpm --filter @sdar/pms-api test
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] 三个包可查询
- [ ] mock 不在生产列表
- [ ] 404/error 稳定

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G1-P5-B02
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G1-P5-B02 --evidence '<command-or-evidence-path>'
```
