# G1-P3-B08 — 抽取 Home Assistant Climate 配置契约

- **阶段**：P3 共享配置契约
- **依赖**：G1-P3-B02
- **推荐提交**：`refactor(climate-config): G1-P3-B08 share climate configuration contract`

## 目标

统一 HA Endpoint、Token、Resource File 等配置。

## 允许修改范围

- `packages/runtime-configuration-contract/src/providers/home-assistant*`
- `apps/home-assistant-climate-provider/src/config.ts`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. 标记 token/file Secret/路径
2. 保留资源文件验证
3. 旧 parseConfig 兼容
4. 补边界测试

## 强制验证

```bash
pnpm --filter @sdar/home-assistant-climate-provider test
```
```bash
pnpm --filter @sdar/runtime-configuration-contract test
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] 旧配置通过
- [ ] Token 不进入日志对象
- [ ] 资源文件错误稳定

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G1-P3-B08
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G1-P3-B08 --evidence '<command-or-evidence-path>'
```
