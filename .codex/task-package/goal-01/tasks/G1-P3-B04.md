# G1-P3-B04 — 抽取 Runtime Observability 配置契约

- **阶段**：P3 共享配置契约
- **依赖**：G1-P3-B02
- **推荐提交**：`refactor(runtime-config): G1-P3-B04 extract observability contract`

## 目标

统一 OTLP、日志、metrics/traces 参数及 Apply Mode。

## 允许修改范围

- `packages/runtime-configuration-contract/src/runtime/observability*`
- `apps/runtime/src/config.ts`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. 映射现有 OTEL_*
2. 开关 hot_reload
3. endpoint reconnect_required
4. TLS/headers SecretRef
5. 保留生产安全验证

## 强制验证

```bash
pnpm --filter @sdar/runtime-configuration-contract test
```
```bash
pnpm test:unit
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] 默认值行为一致
- [ ] Apply Mode 有测试
- [ ] Secret 不进入 effective plain output

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G1-P3-B04
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G1-P3-B04 --evidence '<command-or-evidence-path>'
```
