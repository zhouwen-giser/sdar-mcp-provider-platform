# G1-P5-B08 — 完成 API 安全、OpenAPI 与 P5 阶段门禁

- **阶段**：P5 API 与配置中心
- **依赖**：G1-P5-B02、G1-P5-B03、G1-P5-B07
- **推荐提交**：`test(pms-api): G1-P5-B08 close API configuration phase`

## 目标

补充认证 Port、输入限制、审计、OpenAPI 和配置中心 E2E。

## 允许修改范围

- `apps/pms-api/**`
- `docs/api/**`
- `.codex/reports/phase-P5.md`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. 管理员/只读/runtime token 角色
2. body/JSON size
3. SSRF-safe fields future validation
4. OpenAPI snapshot
5. E2E publish/latest/watch/ack/rollback

## 强制验证

```bash
pnpm test:pms-config-e2e
```
```bash
pnpm --filter @sdar/pms-api test
```
```bash
pnpm build
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] 权限测试通过
- [ ] OpenAPI 完整
- [ ] P5 全部 PASSED

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G1-P5-B08
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G1-P5-B08 --evidence '<command-or-evidence-path>'
```
