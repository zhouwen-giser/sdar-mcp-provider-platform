# G1-P6-B01 — 实现 Runtime Config Client Pull 与本地 Cache

- **阶段**：P6 Runtime Config Client
- **依赖**：G1-P5-B06
- **推荐提交**：`feat(runtime-config-client): G1-P6-B01 implement pull and cache`

## 目标

新增可复用 client，支持 latest/ETag、超时、重试和原子 cache。

## 允许修改范围

- `packages/runtime-config-client/**`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. HTTP port abstraction
2. ETag/304
3. staging file + atomic rename
4. checksum/schema validate
5. 无 PMS 时返回 cache/LKG

## 强制验证

```bash
pnpm --filter @sdar/runtime-config-client test
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] 损坏下载不覆盖 LKG
- [ ] 304 无写入
- [ ] 超时有稳定错误

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G1-P6-B01
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G1-P6-B01 --evidence '<command-or-evidence-path>'
```
