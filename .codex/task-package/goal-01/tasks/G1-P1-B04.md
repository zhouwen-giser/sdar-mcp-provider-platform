# G1-P1-B04 — 实现显式 MigrationSet 解析器

- **阶段**：P1 Migration 隔离
- **依赖**：G1-P1-B03
- **推荐提交**：`feat(migrations): G1-P1-B04 add explicit migration set resolver`

## 目标

新增共享 migration-set resolver，禁止默认扫描根目录。

## 允许修改范围

- `packages/database-migration-runner/**`
- `package.json`
- `tsconfig*.json`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. 定义 runtime/provider:ugv/provider:npc-tank/pms set
2. 解析受控相对目录
3. 拒绝路径穿越和未知 set
4. 返回确定性排序及重复版本检测

## 强制验证

```bash
pnpm --filter @sdar/database-migration-runner test
```
```bash
pnpm typecheck
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] 未知 set 失败关闭
- [ ] 只返回目标目录 SQL
- [ ] 重复序号规则有测试

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G1-P1-B04
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G1-P1-B04 --evidence '<command-or-evidence-path>'
```
