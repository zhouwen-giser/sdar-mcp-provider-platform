# G1-P0-B03 — 建立平台命名与仓库元数据

- **阶段**：P0 基线锁定
- **依赖**：G1-P0-B02
- **推荐提交**：`docs(platform): G1-P0-B03 establish platform repository identity`

## 目标

把产品名称升级为 SDAR MCP Provider Platform，同时保持 workspace package 名和运行入口兼容。

## 允许修改范围

- `README.md`
- `package.json`
- `CHANGELOG.md`
- `docs/architecture/**`
- `.github/**`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. 更新仓库展示名称与说明
2. 新增平台范围/非目标文档
3. 不批量重命名现有 package
4. 记录旧项目名兼容策略

## 强制验证

```bash
pnpm format:check || true
```
```bash
git diff --check
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] README 明确母体工程和新目标
- [ ] packageManager/Node 约束不变
- [ ] 旧运行脚本未删除

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G1-P0-B03
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G1-P0-B03 --evidence '<command-or-evidence-path>'
```
