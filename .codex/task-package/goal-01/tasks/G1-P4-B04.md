# G1-P4-B04 — 实现 PostgreSQL PMS Persistence

- **阶段**：P4 PMS 核心与持久化
- **依赖**：G1-P4-B02、G1-P4-B03
- **推荐提交**：`feat(pms-db): G1-P4-B04 implement PostgreSQL persistence`

## 目标

实现 ports、映射和安全参数化查询。

## 允许修改范围

- `packages/pms-persistence-postgres/**`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. Pool/transaction adapter
2. provider/resource/package repos
3. config/audit/job repos
4. 唯一冲突映射为 domain error
5. 不连接 Runtime DB

## 强制验证

```bash
pnpm --filter @sdar/pms-persistence-postgres test
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] 集成测试通过
- [ ] SQL 参数化
- [ ] 事务回滚正确

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G1-P4-B04
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G1-P4-B04 --evidence '<command-or-evidence-path>'
```
