# G1-P4-B07 — 建立 PMS Worker 基础骨架与 P4 门禁

- **阶段**：P4 PMS 核心与持久化
- **依赖**：G1-P4-B05、G1-P4-B06
- **推荐提交**：`feat(pms-worker): G1-P4-B07 establish worker foundation`

## 目标

新增 Worker 启动、优雅退出、job loop，不实现 PM2。

## 允许修改范围

- `apps/pms-worker/**`
- `docs/operations/**`
- `.codex/reports/phase-P4.md`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. 配置/DB bootstrap
2. job registry
3. SIGTERM
4. health indicator
5. 运行 package sync job

## 强制验证

```bash
pnpm --filter @sdar/pms-worker test
```
```bash
pnpm build
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] Worker 不含 PM2/RuntimeDeployment
- [ ] 可安全启动停止
- [ ] P4 报告完整

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G1-P4-B07
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G1-P4-B07 --evidence '<command-or-evidence-path>'
```
