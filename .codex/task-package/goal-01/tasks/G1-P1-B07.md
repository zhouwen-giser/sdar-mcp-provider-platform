# G1-P1-B07 — 增加 Migration 隔离集成测试

- **阶段**：P1 Migration 隔离
- **依赖**：G1-P1-B05、G1-P1-B06
- **推荐提交**：`test(migrations): G1-P1-B07 prove migration isolation`

## 目标

使用真实 PostgreSQL 或隔离测试库证明三类 Migration 不污染。

## 允许修改范围

- `tests/migration-isolation/**`
- `scripts/**`
- `package.json`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. 准备三库/三 schema fixture
2. 分别执行 runtime/ugv/npc set
3. 断言代表性表存在/不存在
4. 重复执行验证幂等
5. 输出 evidence JSON

## 强制验证

```bash
pnpm test:migration-isolation
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] Runtime 库无 ugv/npc 表
- [ ] UGV/NPC 库无 Task Authority 表
- [ ] 重复迁移无错误

## 完成证据

- `reports/evidence/migration-isolation.json`

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G1-P1-B07
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G1-P1-B07 --evidence '<command-or-evidence-path>'
```
