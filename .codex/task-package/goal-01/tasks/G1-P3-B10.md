# G1-P3-B10 — 完成配置兼容回归与阶段门禁

- **阶段**：P3 共享配置契约
- **依赖**：G1-P3-B09
- **推荐提交**：`test(config-contract): G1-P3-B10 close configuration contract phase`

## 目标

证明抽取前后有效/无效配置行为等价并发布 P3 报告。

## 允许修改范围

- `tests/config-compat/**`
- `.codex/reports/phase-P3.md`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. 构造现有 env fixtures
2. 比较 parse 成功/失败/defaults
3. 明确新增 *_FILE 的兼容规则
4. 运行所有 Provider config tests

## 强制验证

```bash
pnpm test:config-compat
```
```bash
pnpm config:schema:check
```
```bash
pnpm build
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] 无未批准默认值变化
- [ ] 四类配置通过
- [ ] P3 全部 PASSED

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G1-P3-B10
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G1-P3-B10 --evidence '<command-or-evidence-path>'
```
