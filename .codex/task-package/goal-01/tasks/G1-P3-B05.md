# G1-P3-B05 — 抽取 Runtime Worker 与 Business Events 配置

- **阶段**：P3 共享配置契约
- **依赖**：G1-P3-B02
- **推荐提交**：`refactor(runtime-config): G1-P3-B05 extract worker and events contract`

## 目标

统一 Scheduler/Recovery/Command/TTL/Outbox/Business Events/Provider Telemetry。

## 允许修改范围

- `packages/runtime-configuration-contract/src/runtime/**`
- `apps/runtime/src/config.ts`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. 逐项从 inventory 迁移
2. 保守 apply mode
3. 定义容量边界
4. 保持 readiness 策略

## 强制验证

```bash
pnpm --filter @sdar/runtime-configuration-contract test
```
```bash
pnpm test:unit
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] inventory 字段全部覆盖
- [ ] 生产边界不放宽
- [ ] 旧 config parse 回归通过

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G1-P3-B05
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G1-P3-B05 --evidence '<command-or-evidence-path>'
```
