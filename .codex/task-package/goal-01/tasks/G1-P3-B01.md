# G1-P3-B01 — 盘点现有 Runtime 与 Provider 配置

- **阶段**：P3 共享配置契约
- **依赖**：G1-P2-B07
- **推荐提交**：`docs(config): G1-P3-B01 inventory existing configuration`

## 目标

生成配置字段、默认值、生产约束、Secret、Apply Mode 的来源清单。

## 允许修改范围

- `docs/configuration/CONFIG_INVENTORY.json`
- `docs/configuration/CONFIG_INVENTORY.md`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. 分析 runtime/ugv/npc/HA config.ts
2. 不得手工遗漏环境变量
3. 区分 bootstrap/runtime/provider/collector
4. 标记未知 Apply Mode 为 restart_required 待审

## 强制验证

```bash
python3 .codex/task-package/scripts/verify_config_inventory.py
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] 所有现有配置字段有来源
- [ ] Secret 字段可识别
- [ ] 无 silent default change

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G1-P3-B01
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G1-P3-B01 --evidence '<command-or-evidence-path>'
```
