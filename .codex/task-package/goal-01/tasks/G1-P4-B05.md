# G1-P4-B05 — 实现 Provider Package 导入投影

- **阶段**：P4 PMS 核心与持久化
- **依赖**：G1-P2-B07、G1-P4-B04
- **推荐提交**：`feat(pms): G1-P4-B05 synchronize provider packages`

## 目标

把内置 Registry 投影到 PMS DB，保持包文件为来源。

## 允许修改范围

- `packages/pms-application/**`
- `packages/pms-persistence-postgres/**`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. 实现 sync packages use case
2. checksum no-op
3. 禁止 DB 手工内容反向覆盖文件
4. 记录 audit

## 强制验证

```bash
pnpm test:pms
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] 首次导入三个包
- [ ] 重复导入无 revision 噪声
- [ ] 损坏包不部分提交

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G1-P4-B05
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G1-P4-B05 --evidence '<command-or-evidence-path>'
```
