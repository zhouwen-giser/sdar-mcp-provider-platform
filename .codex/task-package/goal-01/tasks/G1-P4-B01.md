# G1-P4-B01 — 建立 PMS Domain 包与核心值对象

- **阶段**：P4 PMS 核心与持久化
- **依赖**：G1-P3-B10
- **推荐提交**：`feat(pms-domain): G1-P4-B01 establish control-plane domain`

## 目标

新增纯领域包，定义 ProviderType/Provider/Resource/Package/Config/Audit。

## 允许修改范围

- `packages/pms-domain/**`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. 定义 branded IDs/status
2. Provider-Resource N:N
3. hostingMode
4. 领域错误和状态转换
5. 不依赖 Fastify/pg

## 强制验证

```bash
pnpm --filter @sdar/pms-domain test
```
```bash
pnpm typecheck
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] 领域包无 infra deps
- [ ] 非法状态被拒绝
- [ ] N:N 模型有测试

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G1-P4-B01
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G1-P4-B01 --evidence '<command-or-evidence-path>'
```
