# G1-P6-B03 — 接入 Runtime 的首个可验证动态配置闭环

- **阶段**：P6 Runtime Config Client
- **依赖**：G1-P3-B04、G1-P6-B02
- **推荐提交**：`feat(runtime): G1-P6-B03 integrate runtime config client`

## 目标

在 apps/runtime 集成 client，优先实现 OTEL_ENABLED 安全动态应用，保持冷启动兼容。

## 允许修改范围

- `apps/runtime/**`
- `packages/runtime-config-client/**`
- `tests/runtime-config-e2e/**`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. 新增 PMS URL/token/cache 可选 bootstrap
2. 默认未配置时行为不变
3. OTEL_ENABLED handler
4. 停止/启用 exporter 不影响 Task Engine
5. 优雅 shutdown

## 强制验证

```bash
pnpm test:pms-config-e2e
```
```bash
pnpm test:runtime-config-e2e
```
```bash
pnpm test:unit
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] 无 PMS 配置仍可启动
- [ ] OTEL 开关 Ack applied
- [ ] Task 相关测试不回退

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G1-P6-B03
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G1-P6-B03 --evidence '<command-or-evidence-path>'
```
