# G1-P6-B02 — 实现 Watch、Apply、Ack 与 LKG 状态机

- **阶段**：P6 Runtime Config Client
- **依赖**：G1-P5-B07、G1-P6-B01
- **推荐提交**：`feat(runtime-config-client): G1-P6-B02 complete apply and LKG workflow`

## 目标

完成 SSE hint→pull→stage→apply→ack→LKG。

## 允许修改范围

- `packages/runtime-config-client/**`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. watch reconnect/backoff
2. apply handler registry by group
3. restart_required 不热应用
4. 成功才更新 active/LKG
5. Ack retry outbox

## 强制验证

```bash
pnpm --filter @sdar/runtime-config-client test
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] PMS 断线继续 LKG
- [ ] apply failure 保留旧配置
- [ ] Ack 状态正确

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G1-P6-B02
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G1-P6-B02 --evidence '<command-or-evidence-path>'
```
