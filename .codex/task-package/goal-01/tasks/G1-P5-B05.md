# G1-P5-B05 — 实现 Config Publish、No-op 与 Rollback

- **阶段**：P5 API 与配置中心
- **依赖**：G1-P5-B04
- **推荐提交**：`feat(config-center): G1-P5-B05 publish and rollback revisions`

## 目标

实现不可变 revision、canonical checksum、激活和回滚。

## 允许修改范围

- `packages/configuration-center/**`
- `packages/pms-persistence-postgres/**`
- `apps/pms-api/**`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. 短事务 publish
2. same checksum no-op
3. rollback 创建或激活明确 revision
4. optimistic concurrency
5. audit

## 强制验证

```bash
pnpm test:pms-config
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] 并发 publish 安全
- [ ] 相同内容不增 revision
- [ ] 历史不可修改

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G1-P5-B05
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G1-P5-B05 --evidence '<command-or-evidence-path>'
```
