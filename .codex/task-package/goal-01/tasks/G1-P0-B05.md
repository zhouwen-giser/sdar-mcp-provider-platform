# G1-P0-B05 — 固化 UGV/NPC/HA Provider 回归门禁

- **阶段**：P0 基线锁定
- **依赖**：G1-P0-B03
- **推荐提交**：`test(provider): G1-P0-B05 lock provider regression gates`

## 目标

识别三个 Provider 及 mock fixtures 的已有验证入口，生成组件资格基线。

## 允许修改范围

- `docs/baseline/**`
- `reports/**`
- `.codex/**`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. 运行可用的 UGV/NPC/HA 单元和契约测试
2. 记录 Mock/Component/Real Resource 状态差异
3. 不得把环境缺口改写为通过

## 强制验证

```bash
pnpm test:unit
```
```bash
pnpm test:contract
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] 三个 Provider 均有基线状态
- [ ] Mock assets 明确排除生产资格
- [ ] 测试证据机器可读

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G1-P0-B05
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G1-P0-B05 --evidence '<command-or-evidence-path>'
```
