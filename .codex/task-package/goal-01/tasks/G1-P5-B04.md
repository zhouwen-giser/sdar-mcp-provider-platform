# G1-P5-B04 — 实现 Config Draft 与 Validate

- **阶段**：P5 API 与配置中心
- **依赖**：G1-P3-B10、G1-P5-B01
- **推荐提交**：`feat(config-center): G1-P5-B04 implement draft validation`

## 目标

用共享契约创建草稿、解析继承并验证。

## 允许修改范围

- `packages/configuration-center/**`
- `apps/pms-api/**`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. 业务键和目标类型
2. create/update draft
3. schema/secret/apply mode validation
4. effective preview
5. 不发布明文 secret

## 强制验证

```bash
pnpm test:pms-config
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] 无效配置不能 publish
- [ ] 继承顺序正确
- [ ] immutable override 拒绝

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G1-P5-B04
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G1-P5-B04 --evidence '<command-or-evidence-path>'
```
