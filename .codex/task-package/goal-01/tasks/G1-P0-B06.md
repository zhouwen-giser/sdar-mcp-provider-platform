# G1-P0-B06 — 发布 P0 基线阶段报告

- **阶段**：P0 基线锁定
- **依赖**：G1-P0-B04、G1-P0-B05
- **推荐提交**：`docs(codex): G1-P0-B06 publish baseline phase report`

## 目标

汇总来源、架构边界、测试门禁和外部环境缺口，解锁 Migration 重构。

## 允许修改范围

- `.codex/reports/phase-P0.md`
- `.codex/decisions.md`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. 使用阶段报告模板
2. 列出可执行/受阻门禁
3. 确认 Runtime/Provider 核心暂不重构
4. 更新任务状态

## 强制验证

```bash
git diff --check
```
```bash
python3 .codex/task-package/scripts/taskctl.py status
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] P0 所有任务 PASSED
- [ ] 报告含 commit 和证据路径
- [ ] 无未解释的测试失败

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G1-P0-B06
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G1-P0-B06 --evidence '<command-or-evidence-path>'
```
