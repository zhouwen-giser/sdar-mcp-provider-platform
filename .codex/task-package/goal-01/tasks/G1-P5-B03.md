# G1-P5-B03 — 实现 Provider/Resource 管理 API

- **阶段**：P5 API 与配置中心
- **依赖**：G1-P5-B01
- **推荐提交**：`feat(pms-api): G1-P5-B03 add provider resource APIs`

## 目标

提供 Provider、ProviderType、Resource、Binding 基础管理。

## 允许修改范围

- `apps/pms-api/**`
- `packages/pms-application/**`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. create/list/get/update status
2. hostingMode 默认 vendor_managed
3. N:N resource binding
4. 所有写操作 audit

## 强制验证

```bash
pnpm --filter @sdar/pms-api test
```
```bash
pnpm test:pms
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] 状态约束有效
- [ ] 无强制 provider.resourceId
- [ ] 审计存在

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G1-P5-B03
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G1-P5-B03 --evidence '<command-or-evidence-path>'
```
