# G1-P2-B01 — 定义 ProviderPackage 领域模型和 JSON Schema

- **阶段**：P2 Provider Package
- **依赖**：G1-P1-B08
- **推荐提交**：`feat(provider-package): G1-P2-B01 define provider package schema`

## 目标

建立版本化 ProviderPackage 模型，保持与任务包 Schema 一致。

## 允许修改范围

- `packages/provider-package-registry/**`
- `schemas/provider-package*.json`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. 定义 Zod/TS 类型
2. 字段含 hostingModes/adapter/runtime/qualification
3. 生成或校验 JSON Schema
4. 禁止额外字段默认漏过

## 强制验证

```bash
pnpm --filter @sdar/provider-package-registry test
```
```bash
pnpm typecheck
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] 合法包通过
- [ ] 缺字段/未知 hosting mode 失败
- [ ] Schema 可供 PMS 使用

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G1-P2-B01
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G1-P2-B01 --evidence '<command-or-evidence-path>'
```
