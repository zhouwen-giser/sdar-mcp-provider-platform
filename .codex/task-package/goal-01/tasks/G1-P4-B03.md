# G1-P4-B03 — 定义 PMS Repository Ports 与事务边界

- **阶段**：P4 PMS 核心与持久化
- **依赖**：G1-P4-B01
- **推荐提交**：`feat(pms-domain): G1-P4-B03 define repository ports`

## 目标

为聚合和配置发布定义 repository/UoW ports。

## 允许修改范围

- `packages/pms-domain/src/ports/**`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. CRUD/query ports
2. optimistic revision
3. transaction callback
4. job lease/audit port
5. 禁止暴露 pg types

## 强制验证

```bash
pnpm --filter @sdar/pms-domain test
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] 接口支持用例
- [ ] 无 infra 类型泄漏
- [ ] 并发前置条件明确

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G1-P4-B03
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G1-P4-B03 --evidence '<command-or-evidence-path>'
```
