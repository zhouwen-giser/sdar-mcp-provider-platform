# G1-P0-B04 — 固化 Runtime 冻结协议回归门禁

- **阶段**：P0 基线锁定
- **依赖**：G1-P0-B03
- **推荐提交**：`test(runtime): G1-P0-B04 lock frozen protocol regression gate`

## 目标

定位并执行可用的 Runtime frozen protocol/conformance 门禁，形成基线证据。

## 允许修改范围

- `package.json`
- `scripts/**`
- `docs/baseline/**`
- `.codex/**`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. 盘点现有 verify/protocol/frozen scripts
2. 先运行无需数据库的冻结协议门禁
3. 数据库不可用时记录精确缺口
4. 不得修改测试绕过失败

## 强制验证

```bash
pnpm protocol:check
```
```bash
pnpm test:frozen-74 || pnpm test -- --runInBand
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] 协议锁校验结果可追踪
- [ ] 74/74 门禁结果或合法外部缺口已记录
- [ ] 未弱化测试

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G1-P0-B04
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G1-P0-B04 --evidence '<command-or-evidence-path>'
```
