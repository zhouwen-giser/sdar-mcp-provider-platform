# G2-P4-B06 — 实现 Registry Snapshot 构建与持久化

- **阶段**：P4 注册、Catalog 与 Registry
- **依赖**：G2-P4-B05
- **推荐提交**：`feat(registry): G2-P4-B06 build registry snapshots`

## 目标

按 environment 生成 SDAR projection，排除敏感/内部字段。

## 允许修改范围

- `packages/registry-snapshot/**`
- `migrations/pms/**`
- `packages/pms-persistence-postgres/**`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. projection contract
2. effective endpoint
3. checksum/no-op
4. history/diff
5. LKG

## 强制验证

```bash
pnpm test:registry
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] 无 Secret/PM2/Task 数据
- [ ] 稳定排序
- [ ] 相同内容 no revision

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G2-P4-B06
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G2-P4-B06 --evidence '<command-or-evidence-path>'
```
