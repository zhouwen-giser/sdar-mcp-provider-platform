# G2-P2-B03 — 定义 PostgresProvisioner Port 与错误模型

- **阶段**：P2 数据库自动准备
- **依赖**：G2-P2-B01
- **推荐提交**：`feat(postgres-provisioner): G2-P2-B03 define provisioning port`

## 目标

定义 inspect/create role/create db/grant/verify/delete guarded port。

## 允许修改范围

- `packages/runtime-deployment/src/ports/**`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. 幂等 spec/result
2. 错误 codes
3. dry-run/inspect
4. 不暴露 admin URL

## 强制验证

```bash
pnpm --filter @sdar/runtime-deployment test
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] 接口可 Fake
- [ ] 删除需要 explicit policy
- [ ] 错误可重试分类

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G2-P2-B03
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G2-P2-B03 --evidence '<command-or-evidence-path>'
```
