# G2-P0-B01 — 验证 Goal 1 Handoff 与仓库门禁

- **阶段**：P0 Goal 2 预检
- **依赖**：无（Goal/阶段入口任务）
- **推荐提交**：`chore(goal2): G2-P0-B01 verify goal 01 handoff`

## 目标

验证 Goal 1 50/50、Handoff、工作树、Migration/config E2E，拒绝不完整基线。

## 允许修改范围

- `.codex/**`
- `docs/baseline/**`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. 运行 handoff verifier
2. 核对 git commit 与 branch
3. 重跑关键 smoke
4. 记录 Goal 2 baseline

## 强制验证

```bash
python3 .codex/task-package/scripts/verify_goal1_handoff.py --repo .
```
```bash
git status --short
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] Handoff 通过
- [ ] 无 FAILED/BLOCKED
- [ ] Goal 2 baseline report 完整

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G2-P0-B01
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G2-P0-B01 --evidence '<command-or-evidence-path>'
```
