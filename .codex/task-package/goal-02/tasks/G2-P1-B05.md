# G2-P1-B05 — 实现 RuntimeDeployment Application Use Cases

- **阶段**：P1 RuntimeDeployment
- **依赖**：G2-P1-B04
- **推荐提交**：`feat(pms): G2-P1-B05 add deployment use cases`

## 目标

创建/启动/停止/重启/扩缩/对账意图用例，仅写期望状态。

## 允许修改范围

- `packages/pms-application/**`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. create deployment validates Provider/config/profile
2. commands enqueue jobs
3. stop/drain semantics
4. desiredReplicas V0.1 guard

## 强制验证

```bash
pnpm test:pms
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] API use case 不直接调用 PM2
- [ ] 无稳定 gateway 时 replicas>1 拒绝或 feature flag
- [ ] audit 存在

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G2-P1-B05
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G2-P1-B05 --evidence '<command-or-evidence-path>'
```
