# G2-P2-B07 — 实现数据库准备 Application Job

- **阶段**：P2 数据库自动准备
- **依赖**：G2-P2-B05、G2-P2-B06
- **推荐提交**：`feat(pms-worker): G2-P2-B07 orchestrate runtime database setup`

## 目标

把 Deployment 状态、Provisioner、Secret、Migration 串成可恢复 Job。

## 允许修改范围

- `packages/pms-application/**`
- `apps/pms-worker/**`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. DATABASE_PROVISIONING/MIGRATING transitions
2. 步骤 checkpoint
3. 幂等重试
4. audit/error mapping

## 强制验证

```bash
pnpm test:db-provisioner
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] 失败停在明确状态
- [ ] 重试不重复破坏
- [ ] 网络调用无长事务

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G2-P2-B07
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G2-P2-B07 --evidence '<command-or-evidence-path>'
```
