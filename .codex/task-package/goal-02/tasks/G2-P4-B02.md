# G2-P4-B02 — 实现 Runtime Registration/Heartbeat API 与 Client

- **阶段**：P4 注册、Catalog 与 Registry
- **依赖**：G2-P4-B01
- **推荐提交**：`feat(runtime-registration): G2-P4-B02 connect runtime heartbeat`

## 目标

PMS API 接收，Runtime 客户端发送，使用最小权限 token。

## 允许修改范围

- `apps/pms-api/**`
- `apps/runtime/**`
- `packages/runtime-registration/**`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. register endpoint
2. heartbeat endpoint
3. auth/correlation
4. backoff
5. PMS 不可用不停止 Runtime

## 强制验证

```bash
pnpm test:runtime-registration
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] PMS outage tolerated
- [ ] Token scope 正确
- [ ] 状态更新审计

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G2-P4-B02
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G2-P4-B02 --evidence '<command-or-evidence-path>'
```
