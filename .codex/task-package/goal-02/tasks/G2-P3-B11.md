# G2-P3-B11 — 实现停止与排空流程

- **阶段**：P3 PM2 与运行对账
- **依赖**：G2-P3-B09
- **推荐提交**：`feat(runtime-lifecycle): G2-P3-B11 implement draining and shutdown`

## 目标

停止新调用、优雅退出、超时强制停止，避免 Task Authority 切换。

## 允许修改范围

- `apps/runtime/**`
- `packages/pm2-runtime-adapter/**`
- `packages/runtime-deployment/**`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. Runtime SIGTERM cleanup
2. DRAINING model
3. V0.1 active task policy
4. kill_timeout
5. DB switch all-stop guard

## 强制验证

```bash
pnpm test:runtime-reconcile
```
```bash
pnpm --filter @sdar/runtime test
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] SIGTERM 释放资源
- [ ] 数据库切换不滚动混用
- [ ] 超时有证据

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G2-P3-B11
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G2-P3-B11 --evidence '<command-or-evidence-path>'
```
