# G2-P0-B04 — 建立 Goal 2 阶段门禁与报告骨架

- **阶段**：P0 Goal 2 预检
- **依赖**：G2-P0-B02、G2-P0-B03
- **推荐提交**：`chore(goal2): G2-P0-B04 establish governance gates`

## 目标

配置 Goal 2 scripts/test commands/报告目录。

## 允许修改范围

- `package.json`
- `scripts/**`
- `.codex/reports/**`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. 新增 test:db-provisioner/test:pm2-adapter/test:runtime-deployment/test:registry/test:platform-e2e placeholders 必须真实失败或 no-test guard
2. 不添加永远成功空脚本
3. 阶段报告模板

## 强制验证

```bash
pnpm --version
```
```bash
python3 .codex/task-package/scripts/taskctl.py status
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] 命令名称明确
- [ ] 空测试不得伪装通过
- [ ] P0 完成

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G2-P0-B04
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G2-P0-B04 --evidence '<command-or-evidence-path>'
```
