# G2-P3-B04 — 实现 Runtime 版本与入口解析

- **阶段**：P3 PM2 与运行对账
- **依赖**：G2-P3-B02
- **推荐提交**：`feat(pm2-adapter): G2-P3-B04 secure runtime release resolution`

## 目标

只允许受控 release root 和固定 Runtime entry。

## 允许修改范围

- `packages/pm2-runtime-adapter/src/releases/**`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. version manifest
2. path containment
3. entry exists/executable/readable
4. 禁止任意 script/cwd
5. current baseline version mapping

## 强制验证

```bash
pnpm --filter @sdar/pm2-runtime-adapter test
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] 路径穿越拒绝
- [ ] 未知版本失败
- [ ] 不执行用户命令

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G2-P3-B04
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G2-P3-B04 --evidence '<command-or-evidence-path>'
```
