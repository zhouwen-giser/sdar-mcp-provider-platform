# G2-P4-B05 — 持久化不可变 CatalogSnapshot

- **阶段**：P4 注册、Catalog 与 Registry
- **依赖**：G2-P4-B04
- **推荐提交**：`feat(catalog): G2-P4-B05 persist catalog snapshots`

## 目标

追加 PMS Migration/repo，checksum no-op。

## 允许修改范围

- `migrations/pms/**`
- `packages/pms-persistence-postgres/**`
- `packages/catalog-manager/**`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. catalog revision/checksum/json
2. active pointer
3. same checksum no-op
4. history/diff
5. audit

## 强制验证

```bash
pnpm test:catalog
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] no-op 不增 revision
- [ ] Schema 不可管理员修改
- [ ] 历史不可变

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G2-P4-B05
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G2-P4-B05 --evidence '<command-or-evidence-path>'
```
