# G2-P3-B09 — 实现 RuntimeDeployment Reconcile Worker

- **阶段**：P3 PM2 与运行对账
- **依赖**：G2-P2-B07、G2-P3-B06、G2-P3-B07
- **推荐提交**：`feat(pms-worker): G2-P3-B09 reconcile runtime deployments`

## 目标

比较 desired/actual，推进数据库、配置、启动、健康状态。

## 允许修改范围

- `apps/pms-worker/**`
- `packages/pms-application/**`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. job lease
2. stepwise state machine
3. retry/backoff
4. orphan detection
5. stop desired state

## 强制验证

```bash
pnpm test:runtime-reconcile
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] 重复 reconcile 收敛
- [ ] PMS DB 事务短
- [ ] 失败不误删 DB

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G2-P3-B09
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G2-P3-B09 --evidence '<command-or-evidence-path>'
```
