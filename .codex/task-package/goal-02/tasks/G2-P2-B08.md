# G2-P2-B08 — 增加数据库权限与隔离 E2E

- **阶段**：P2 数据库自动准备
- **依赖**：G2-P2-B07
- **推荐提交**：`test(database): G2-P2-B08 prove provisioning isolation`

## 目标

证明 Provider A/B、PMS、Provisioning 凭据边界。

## 允许修改范围

- `tests/database-provisioning-e2e/**`
- `reports/evidence/**`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. 创建两 provider db
2. Runtime A 无法访问 B
3. PMS credential 无 Task DB 权限
4. runtime migration succeeds
5. 输出 JSON

## 强制验证

```bash
pnpm test:db-provisioner-e2e
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] 隔离断言通过
- [ ] Secret 输出脱敏
- [ ] 清理可控

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G2-P2-B08
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G2-P2-B08 --evidence '<command-or-evidence-path>'
```
