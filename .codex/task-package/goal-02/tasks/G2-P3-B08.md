# G2-P3-B08 — 扩展 Runtime Bootstrap 身份与 Secret File 支持

- **阶段**：P3 PM2 与运行对账
- **依赖**：G2-P3-B02
- **推荐提交**：`feat(runtime): G2-P3-B08 support platform bootstrap identity`

## 目标

让 Runtime 支持 PMS_INSTANCE_ID/PMS_DEPLOYMENT_ID/DATABASE_URL_FILE 等。

## 允许修改范围

- `apps/runtime/src/config.ts`
- `apps/runtime/**`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. file/env priority and conflict
2. stable identity
3. Secret file read errors
4. 旧 DATABASE_URL 兼容
5. 不把 identity 远程覆盖

## 强制验证

```bash
pnpm --filter @sdar/runtime test
```
```bash
pnpm test:config-compat
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] 旧启动不回退
- [ ] 文件配置成功
- [ ] 冲突规则测试

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G2-P3-B08
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G2-P3-B08 --evidence '<command-or-evidence-path>'
```
