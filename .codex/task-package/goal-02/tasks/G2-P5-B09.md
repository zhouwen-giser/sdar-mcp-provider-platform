# G2-P5-B09 — 完成 V0.1 全量验收、发布与交付

- **阶段**：P5 Provider 集成与交付
- **依赖**：G2-P5-B06、G2-P5-B07、G2-P5-B08
- **推荐提交**：`chore(release): G2-P5-B09 deliver provider platform v0.1`

## 目标

运行 verify:platform、生成发布清单、升级说明、运维手册、最终报告和 Handoff。

## 允许修改范围

- `CHANGELOG.md`
- `docs/operations/**`
- `reports/platform-v0.1/**`
- `.codex/handoff/**`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. 运行全量门禁
2. 生成 SBOM/manifest/checksums 如现有工具支持
3. 版本兼容矩阵
4. 已知限制
5. 最终 task 50/50

## 强制验证

```bash
pnpm verify:platform
```
```bash
python3 .codex/task-package/scripts/taskctl.py status
```
```bash
git status --short
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] Goal 2 50 tasks PASSED（外部 Interop 如阻断必须在最终状态明确，不称完整认证）
- [ ] 所有核心 V0.1 门禁通过
- [ ] 交付报告可复现

## 完成证据

- `reports/platform-v0.1/FINAL_DELIVERY_REPORT.md`

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G2-P5-B09
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G2-P5-B09 --evidence '<command-or-evidence-path>'
```
