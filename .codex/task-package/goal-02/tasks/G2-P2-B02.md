# G2-P2-B02 — 增加 DatabaseProfile PMS Migration 与 Repository

- **阶段**：P2 数据库自动准备
- **依赖**：G2-P2-B01
- **推荐提交**：`feat(pms-db): G2-P2-B02 persist database profiles`

## 目标

持久化 Profile、provision result 与 credential refs。

## 允许修改范围

- `migrations/pms/**`
- `packages/pms-persistence-postgres/**`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. 追加表/索引
2. SecretRef only
3. 状态/last error
4. repo tests

## 强制验证

```bash
pnpm test:pms-migrations
```
```bash
pnpm test:pms
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] 无明文 Secret 列
- [ ] Provider/Environment 作用域
- [ ] 审计关联

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G2-P2-B02
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G2-P2-B02 --evidence '<command-or-evidence-path>'
```
