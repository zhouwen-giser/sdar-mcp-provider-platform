# G2-P0-B03 — 冻结 Goal 2 领域决策与 ADR

- **阶段**：P0 Goal 2 预检
- **依赖**：G2-P0-B01
- **推荐提交**：`docs(adr): G2-P0-B03 freeze runtime governance decisions`

## 目标

创建/确认 PM2 Fork、单节点、vendor-managed default、single replica default 等 ADR。

## 允许修改范围

- `docs/adr/**`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. ADR 同仓不同进程
2. ADR PM2 vs K8s
3. ADR DB per Provider
4. ADR ClickHouse collector
5. ADR multi-replica gateway deferred

## 强制验证

```bash
git diff --check
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] ADR 状态 accepted
- [ ] 与总体设计一致
- [ ] 无隐式范围扩张

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G2-P0-B03
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G2-P0-B03 --evidence '<command-or-evidence-path>'
```
