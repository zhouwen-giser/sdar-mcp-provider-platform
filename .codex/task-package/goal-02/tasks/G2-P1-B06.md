# G2-P1-B06 — 实现 RuntimeDeployment 管理 API

- **阶段**：P1 RuntimeDeployment
- **依赖**：G2-P1-B05
- **推荐提交**：`feat(pms-api): G2-P1-B06 expose runtime deployments`

## 目标

提供 CRUD/action/status API 与 OpenAPI。

## 允许修改范围

- `apps/pms-api/**`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. POST/GET/list
2. start/stop/restart/scale/reconcile
3. operation ID
4. validation/audit

## 强制验证

```bash
pnpm --filter @sdar/pms-api test
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] API 只表达期望状态
- [ ] 错误 code 稳定
- [ ] OpenAPI 更新

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G2-P1-B06
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G2-P1-B06 --evidence '<command-or-evidence-path>'
```
