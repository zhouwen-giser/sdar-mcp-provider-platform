# G2-P3-B02 — 实现 BootstrapConfigRenderer

- **阶段**：P3 PM2 与运行对账
- **依赖**：G2-P2-B05、G2-P3-B01
- **推荐提交**：`feat(pm2-adapter): G2-P3-B02 render bootstrap config`

## 目标

解析 effective config，生成环境变量白名单和 Secret 文件引用。

## 允许修改范围

- `packages/pm2-runtime-adapter/src/bootstrap/**`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. immutable identity
2. PORT allocation input
3. DATABASE_URL_FILE/PMS token
4. allowlist
5. redacted preview

## 强制验证

```bash
pnpm --filter @sdar/pm2-runtime-adapter test
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] 无明文 Secret
- [ ] 未知 env 拒绝
- [ ] 输出确定性

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G2-P3-B02
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G2-P3-B02 --evidence '<command-or-evidence-path>'
```
