# G2-P5-B01 — 集成 vendor_managed UGV Runtime 部署

- **阶段**：P5 Provider 集成与交付
- **依赖**：G2-P4-B08
- **推荐提交**：`test(platform): G2-P5-B01 integrate UGV provider`

## 目标

使用已运行/Mock UGV Adapter，平台自动建 Runtime DB、启动 Runtime、发现 9 Operations。

## 允许修改范围

- `tests/provider-platform-e2e/ugv/**`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. 创建 Provider/package binding
2. vendor adapter endpoint
3. deployment start
4. ready/identity/catalog
5. registry

## 强制验证

```bash
pnpm test:provider-platform-ugv
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] 9 Operations 或基线实际 catalog 一致
- [ ] Provider DB 不由平台创建
- [ ] 全链证据

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G2-P5-B01
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G2-P5-B01 --evidence '<command-or-evidence-path>'
```
