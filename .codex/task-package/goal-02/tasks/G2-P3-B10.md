# G2-P3-B10 — 实现崩溃恢复与重启策略

- **阶段**：P3 PM2 与运行对账
- **依赖**：G2-P3-B09
- **推荐提交**：`feat(pm2-adapter): G2-P3-B10 add controlled crash recovery`

## 目标

配置 restartDelay/maxRestarts/maxMemory 并同步 observed state。

## 允许修改范围

- `packages/pm2-runtime-adapter/**`
- `apps/pms-worker/**`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. policy config limits
2. errored loop handling
3. restart count
4. manual intervention state
5. Runtime persistent recovery E2E

## 强制验证

```bash
pnpm test:pm2-adapter
```
```bash
pnpm test:runtime-reconcile
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] 不会无限快速重启
- [ ] 状态 DEGRADED/FAILED 合理
- [ ] Task DB 不清理

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G2-P3-B10
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G2-P3-B10 --evidence '<command-or-evidence-path>'
```
