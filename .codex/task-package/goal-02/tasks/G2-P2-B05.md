# G2-P2-B05 — 实现 File Secret Store Adapter

- **阶段**：P2 数据库自动准备
- **依赖**：G2-P2-B04
- **推荐提交**：`feat(secret-store): G2-P2-B05 add file secret adapter`

## 目标

V0.1 提供受控本地 Secret 文件实现和可替换 Port。

## 允许修改范围

- `packages/secret-store/**`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. write atomic 0600
2. deployment/instance path containment
3. read ref
4. cleanup policy
5. redacted inspect

## 强制验证

```bash
pnpm --filter @sdar/secret-store test
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] 权限测试
- [ ] 路径穿越拒绝
- [ ] 日志无内容

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G2-P2-B05
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G2-P2-B05 --evidence '<command-or-evidence-path>'
```
