# G2-P4-B03 — 实现 Provider 身份三方校验

- **阶段**：P4 注册、Catalog 与 Registry
- **依赖**：G2-P4-B02
- **推荐提交**：`feat(provider-identity): G2-P4-B03 enforce identity consistency`

## 目标

校验 PMS providerId=bootstrap=Adapter Manifest。

## 允许修改范围

- `packages/pms-application/**`
- `apps/runtime/**`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. 读取 DescribeProvider evidence
2. 结构化 PROVIDER_ID_MISMATCH
3. 阻断 ACTIVE
4. 不自动重写身份

## 强制验证

```bash
pnpm test:runtime-registration
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] 三种 mismatch case 覆盖
- [ ] 日志脱敏
- [ ] 恢复后可重试

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G2-P4-B03
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G2-P4-B03 --evidence '<command-or-evidence-path>'
```
