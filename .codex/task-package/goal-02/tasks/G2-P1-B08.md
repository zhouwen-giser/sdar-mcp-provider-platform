# G2-P1-B08 — 完成 RuntimeDeployment 契约与阶段门禁

- **阶段**：P1 RuntimeDeployment
- **依赖**：G2-P1-B06、G2-P1-B07
- **推荐提交**：`test(runtime-deployment): G2-P1-B08 close deployment phase`

## 目标

补 Schema、契约测试和 P1 报告。

## 允许修改范围

- `schemas/runtime-deployment*.json`
- `tests/runtime-deployment/**`
- `.codex/reports/goal2-P1.md`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. 对齐任务包 Schema
2. API/domain/repo contract tests
3. 状态机 property tests
4. 报告

## 强制验证

```bash
pnpm test:runtime-deployment
```
```bash
pnpm build
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] P1 全部通过
- [ ] 状态机与 API 一致
- [ ] 无 infra 越界

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G2-P1-B08
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G2-P1-B08 --evidence '<command-or-evidence-path>'
```
