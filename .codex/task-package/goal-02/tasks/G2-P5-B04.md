# G2-P5-B04 — 实现 PMS Web 基础框架与 Provider 页面

- **阶段**：P5 Provider 集成与交付
- **依赖**：G2-P1-B07
- **推荐提交**：`feat(pms-web): G2-P5-B04 add provider management UI`

## 目标

新增管理 Web 的路由、API client、Provider/Package/Resource 页面。

## 允许修改范围

- `apps/pms-web/**`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. 沿用项目 UI 约束或最小 React/Vite
2. 列表/详情/create
3. qualification badges
4. 错误/loading
5. 不直接 DB

## 强制验证

```bash
pnpm --filter @sdar/pms-web test
```
```bash
pnpm --filter @sdar/pms-web build
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] 页面可用
- [ ] Mock/Real 状态清晰
- [ ] 无 Secret 展示

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G2-P5-B04
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G2-P5-B04 --evidence '<command-or-evidence-path>'
```
