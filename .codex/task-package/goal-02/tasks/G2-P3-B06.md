# G2-P3-B06 — 实现 Runtime Start/Stop/Restart/Delete

- **阶段**：P3 PM2 与运行对账
- **依赖**：G2-P3-B02、G2-P3-B03、G2-P3-B04、G2-P3-B05
- **推荐提交**：`feat(pm2-adapter): G2-P3-B06 manage runtime lifecycle`

## 目标

实现进程生命周期与状态持久化。

## 允许修改范围

- `packages/pm2-runtime-adapter/**`
- `apps/pms-worker/**`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. start spec
2. wait online
3. stop graceful timeout
4. restart updateEnv controlled
5. delete cleanup Secret policy
6. audit

## 强制验证

```bash
pnpm test:pm2-adapter
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] 四类操作幂等
- [ ] 只 Fork Mode
- [ ] 过程状态可追踪

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G2-P3-B06
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G2-P3-B06 --evidence '<command-or-evidence-path>'
```
