# G2-P2-B04 — 实现 PostgreSQL Provisioner

- **阶段**：P2 数据库自动准备
- **依赖**：G2-P2-B02、G2-P2-B03
- **推荐提交**：`feat(postgres-provisioner): G2-P2-B04 implement database provisioning`

## 目标

以受限管理凭据创建 Runtime DB/Role/权限。

## 允许修改范围

- `packages/postgres-provisioner/**`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. safe identifier quoting
2. database/role exists handling
3. rotate credential hook
4. grant minimal permissions
5. verify SELECT/DDL

## 强制验证

```bash
pnpm --filter @sdar/postgres-provisioner test
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] 重复执行幂等
- [ ] SQL injection tests
- [ ] 不返回 admin credential

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G2-P2-B04
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G2-P2-B04 --evidence '<command-or-evidence-path>'
```
