# G2-P0-B02 — 确认 PM2/PostgreSQL 环境与 Fake 策略

- **阶段**：P0 Goal 2 预检
- **依赖**：G2-P0-B01
- **推荐提交**：`docs(goal2): G2-P0-B02 inventory runtime infrastructure environment`

## 目标

盘点 PM2、本机 Node、PostgreSQL 管理凭据、端口与可替代测试环境。

## 允许修改范围

- `docs/baseline/GOAL2_ENVIRONMENT.json`
- `.codex/blockers.md`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. 检查 pm2 --version/JS API
2. 检查 PostgreSQL URL 或测试容器可用性
3. 定义 FakePm2/FakeProvisioner 用于单元
4. 不因 Docker 不可用阻断全部 Goal

## 强制验证

```bash
node --version
```
```bash
pnpm --version
```
```bash
pm2 --version || true
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] 环境状态明确
- [ ] 真实 E2E 条件缺口有解除动作
- [ ] 单元开发可继续

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G2-P0-B02
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G2-P0-B02 --evidence '<command-or-evidence-path>'
```
