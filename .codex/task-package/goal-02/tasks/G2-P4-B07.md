# G2-P4-B07 — 实现 Registry API/Watch/Bootstrap 与 SDAR 适配材料

- **阶段**：P4 注册、Catalog 与 Registry
- **依赖**：G2-P4-B06
- **推荐提交**：`feat(registry-api): G2-P4-B07 expose registry snapshots`

## 目标

提供 latest/history/diff/watch/bootstrap，输出当前 SDAR 可消费形态。

## 允许修改范围

- `apps/pms-api/**`
- `docs/integration/SDAR_REGISTRY_IMPORT.md`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. ETag/304
2. SSE revision hint
3. bootstrap fallback
4. credential ref 不明文
5. 说明单 endpoint 约束

## 强制验证

```bash
pnpm test:registry-e2e
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] API 契约通过
- [ ] no-op stable
- [ ] SDAR import fixture 可验证

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G2-P4-B07
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G2-P4-B07 --evidence '<command-or-evidence-path>'
```
