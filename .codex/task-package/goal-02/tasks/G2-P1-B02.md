# G2-P1-B02 — 定义 RuntimeProcess 投影与实际状态

- **阶段**：P1 RuntimeDeployment
- **依赖**：G2-P1-B01
- **推荐提交**：`feat(runtime-deployment): G2-P1-B02 define process projection`

## 目标

区分 processState/readiness/registration/catalog/config。

## 允许修改范围

- `packages/runtime-deployment/**`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. 字段 pm2/pid/port/heartbeat/version
2. 计算 observed health
3. PM2 online 不等于 ACTIVE
4. stale heartbeat

## 强制验证

```bash
pnpm --filter @sdar/runtime-deployment test
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] 状态组合测试完整
- [ ] 实例身份稳定
- [ ] 无临时 PID 作为 identity

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G2-P1-B02
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G2-P1-B02 --evidence '<command-or-evidence-path>'
```
