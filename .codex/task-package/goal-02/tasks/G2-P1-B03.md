# G2-P1-B03 — 增加 RuntimeDeployment PMS Migration

- **阶段**：P1 RuntimeDeployment
- **依赖**：G2-P1-B01、G2-P1-B02
- **推荐提交**：`feat(pms-db): G2-P1-B03 persist runtime deployment`

## 目标

新增 deployment/process/action history 表与约束。

## 允许修改范围

- `migrations/pms/**`
- `docs/database/PMS_SCHEMA.md`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. 追加 migration
2. provider FK
3. desired/observed revision
4. unique pm2Name/instanceId/port scope
5. action history

## 强制验证

```bash
pnpm test:pms-migrations
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] 升级现有 PMS DB 成功
- [ ] 无修改旧 migration
- [ ] 约束覆盖

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G2-P1-B03
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G2-P1-B03 --evidence '<command-or-evidence-path>'
```
