# G2-P5-B03 — 集成 Home Assistant Climate Runtime 部署

- **阶段**：P5 Provider 集成与交付
- **依赖**：G2-P4-B08
- **推荐提交**：`test(platform): G2-P5-B03 integrate climate provider`

## 目标

验证非车辆 Provider 的平台通用性。

## 允许修改范围

- `tests/provider-platform-e2e/home-assistant/**`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. vendor managed adapter/mock fixture
2. Runtime DB
3. catalog
4. registry
5. resource binding

## 强制验证

```bash
pnpm test:provider-platform-ha
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] 无车辆硬编码
- [ ] 资格 pending
- [ ] Catalog 正确

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G2-P5-B03
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G2-P5-B03 --evidence '<command-or-evidence-path>'
```
