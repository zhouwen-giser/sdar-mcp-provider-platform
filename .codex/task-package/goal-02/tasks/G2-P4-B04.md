# G2-P4-B04 — 实现 Catalog Discovery Client 与验证

- **阶段**：P4 注册、Catalog 与 Registry
- **依赖**：G2-P4-B03
- **推荐提交**：`feat(catalog): G2-P4-B04 discover runtime catalog`

## 目标

Runtime ready 后执行正式 discover/tools list，验证 frozen profile。

## 允许修改范围

- `packages/catalog-manager/**`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. server/discover
2. tools/list complete bounded
3. schema/task profile/resource binding validation
4. timeout/retry
5. canonicalize

## 强制验证

```bash
pnpm --filter @sdar/catalog-manager test
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] 拒绝 nextCursor/非法 schema
- [ ] 排序稳定
- [ ] 错误不部分提交

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G2-P4-B04
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G2-P4-B04 --evidence '<command-or-evidence-path>'
```
