# G2-P5-B07 — 完成平台安全与故障注入测试

- **阶段**：P5 Provider 集成与交付
- **依赖**：G2-P5-B01、G2-P5-B02、G2-P5-B03
- **推荐提交**：`test(platform): G2-P5-B07 harden security and recovery`

## 目标

覆盖 Secret、PM2 任意命令、SSRF、DB、PMS/Adapter/Runtime 故障。

## 允许修改范围

- `tests/security-platform/**`
- `tests/fault-injection/**`
- `reports/evidence/**`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. Secret log scan
2. path/script injection
3. Adapter endpoint policy
4. PMS outage LKG
5. Runtime kill recovery
6. migration failure

## 强制验证

```bash
pnpm test:platform-security
```
```bash
pnpm test:fault-injection
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] 关键攻击被拒绝
- [ ] PMS outage 不停 Task
- [ ] 证据完整

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G2-P5-B07
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G2-P5-B07 --evidence '<command-or-evidence-path>'
```
