# G2-P3-B05 — 实现端口分配与实例命名

- **阶段**：P3 PM2 与运行对账
- **依赖**：G2-P1-B02
- **推荐提交**：`feat(runtime-deployment): G2-P3-B05 allocate instance identity and ports`

## 目标

提供稳定 instanceId、pm2Name 和端口租约。

## 允许修改范围

- `packages/runtime-deployment/**`
- `packages/pms-persistence-postgres/**`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. slug normalization
2. ordinal
3. port range/lease/conflict
4. 重试后 identity 不变

## 强制验证

```bash
pnpm test:runtime-deployment
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] 不同实例不冲突
- [ ] 非法 providerId 不注入名称
- [ ] 释放/重用策略明确

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G2-P3-B05
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G2-P3-B05 --evidence '<command-or-evidence-path>'
```
