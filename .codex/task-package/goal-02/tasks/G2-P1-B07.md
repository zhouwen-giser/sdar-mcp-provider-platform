# G2-P1-B07 — 实现 RuntimeProcess 查询与日志引用 API

- **阶段**：P1 RuntimeDeployment
- **依赖**：G2-P1-B04
- **推荐提交**：`feat(pms-api): G2-P1-B07 expose runtime process status`

## 目标

提供实例状态，不直接返回任意文件内容。

## 允许修改范围

- `apps/pms-api/**`
- `packages/pms-application/**`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. list/detail/filter
2. 日志只返回受控 reference/尾部 API 预留
3. 隐藏 Secret/env
4. stale 状态

## 强制验证

```bash
pnpm --filter @sdar/pms-api test
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] 无任意路径读取
- [ ] PID/PM2 状态可见
- [ ] Secret 不泄漏

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G2-P1-B07
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G2-P1-B07 --evidence '<command-or-evidence-path>'
```
