# G2-P4-B08 — 完成 Ready→Catalog→Registry 自动编排门禁

- **阶段**：P4 注册、Catalog 与 Registry
- **依赖**：G2-P4-B07
- **推荐提交**：`test(catalog-registry): G2-P4-B08 close discovery publication phase`

## 目标

Reconcile 在身份/ready 通过后发现 Catalog 并发布 Registry。

## 允许修改范围

- `apps/pms-worker/**`
- `tests/catalog-registry-e2e/**`
- `.codex/reports/goal2-P4.md`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. HEALTH_CHECKING→DISCOVERING→ACTIVE
2. catalog drift handling
3. registry publish after commit
4. failure rollback observed state

## 强制验证

```bash
pnpm test:catalog-registry-e2e
```
```bash
pnpm build
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] ACTIVE 必须有有效 catalog
- [ ] 发现失败不发布 registry
- [ ] P4 报告完整

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G2-P4-B08
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G2-P4-B08 --evidence '<command-or-evidence-path>'
```
