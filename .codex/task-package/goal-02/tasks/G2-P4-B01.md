# G2-P4-B01 — 定义 Runtime Registration 与 Heartbeat 模型

- **阶段**：P4 注册、Catalog 与 Registry
- **依赖**：G2-P3-B12
- **推荐提交**：`feat(runtime-registration): G2-P4-B01 define registration model`

## 目标

建立预期实例注册、心跳和版本/readiness 状态。

## 允许修改范围

- `packages/runtime-registration/**`
- `packages/pms-domain/**`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. registration DTO/validation
2. 不允许注册创建任意 Provider
3. heartbeat TTL
4. version/protocol/config revision

## 强制验证

```bash
pnpm --filter @sdar/runtime-registration test
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] 身份 mismatch 拒绝
- [ ] 重复注册幂等
- [ ] stale 可检测

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G2-P4-B01
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G2-P4-B01 --evidence '<command-or-evidence-path>'
```
