# G2-P1-B04 — 实现 Deployment Repository 与并发更新

- **阶段**：P1 RuntimeDeployment
- **依赖**：G2-P1-B03
- **推荐提交**：`feat(pms-db): G2-P1-B04 implement deployment repositories`

## 目标

实现聚合、process 投影和 compare-and-set。

## 允许修改范围

- `packages/pms-persistence-postgres/**`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. load/save with expectedRevision
2. process upsert
3. action append
4. transaction tests

## 强制验证

```bash
pnpm --filter @sdar/pms-persistence-postgres test
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] 并发冲突稳定
- [ ] 重复 heartbeat 幂等
- [ ] 跨 provider 查询隔离

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G2-P1-B04
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G2-P1-B04 --evidence '<command-or-evidence-path>'
```
