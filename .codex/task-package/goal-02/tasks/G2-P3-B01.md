# G2-P3-B01 — 定义 RuntimeInfrastructureAdapter Port

- **阶段**：P3 PM2 与运行对账
- **依赖**：G2-P2-B09
- **推荐提交**：`feat(runtime-infra): G2-P3-B01 define infrastructure adapter`

## 目标

建立 DB/Migration/Bootstrap/Process/Health 统一内部 Port，但保持组合实现。

## 允许修改范围

- `packages/runtime-deployment/src/ports/**`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. 定义 methods/results/errors/context
2. idempotency/correlation
3. timeout/cancel
4. 不对外 HTTP 任意命令

## 强制验证

```bash
pnpm --filter @sdar/runtime-deployment test
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] Port 可用 fake
- [ ] 无 PM2 types 泄漏
- [ ] 步骤状态明确

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G2-P3-B01
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G2-P3-B01 --evidence '<command-or-evidence-path>'
```
