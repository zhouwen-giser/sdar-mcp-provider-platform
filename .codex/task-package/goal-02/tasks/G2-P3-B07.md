# G2-P3-B07 — 实现 Runtime Health Probe

- **阶段**：P3 PM2 与运行对账
- **依赖**：G2-P3-B06
- **推荐提交**：`feat(runtime-health): G2-P3-B07 probe live and readiness`

## 目标

探测 PM2、health/live、health/ready，分类依赖失败。

## 允许修改范围

- `packages/pm2-runtime-adapter/src/health/**`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. HTTP timeout
2. JSON schema
3. live/ready distinct
4. Adapter/DB failure mapping
5. SSRF allowlist endpoint

## 强制验证

```bash
pnpm test:pm2-adapter
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] PM2 online+ready 才健康
- [ ] 超时不挂 worker
- [ ] 错误脱敏

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G2-P3-B07
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G2-P3-B07 --evidence '<command-or-evidence-path>'
```
