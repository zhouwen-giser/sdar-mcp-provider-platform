# G2-P1-B01 — 定义 RuntimeDeployment 聚合与状态机

- **阶段**：P1 RuntimeDeployment
- **依赖**：G2-P0-B04
- **推荐提交**：`feat(runtime-deployment): G2-P1-B01 define deployment aggregate`

## 目标

建立期望状态、revision 和合法状态转移。

## 允许修改范围

- `packages/runtime-deployment/**`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. 定义 IDs/spec/status
2. 实现 REQUESTED→...→ACTIVE
3. expectedRevision 并发控制
4. 失败/停止/排空状态
5. 领域事件

## 强制验证

```bash
pnpm --filter @sdar/runtime-deployment test
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] 非法跳转拒绝
- [ ] 重复动作幂等
- [ ] domain 无 PM2/pg deps

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G2-P1-B01
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G2-P1-B01 --evidence '<command-or-evidence-path>'
```
