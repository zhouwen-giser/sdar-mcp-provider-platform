# G2-P2-B06 — 实现 Runtime Migration Runner 编排

- **阶段**：P2 数据库自动准备
- **依赖**：G2-P2-B04
- **推荐提交**：`feat(runtime-migration): G2-P2-B06 add migration orchestration`

## 目标

按 runtimeVersion 和 runtime set 在启动前执行迁移。

## 允许修改范围

- `packages/runtime-migration-runner/**`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. 使用现有 migration engine
2. advisory lock
3. version/checksum inspect
4. timeout/error evidence
5. 不做 rollback SQL

## 强制验证

```bash
pnpm --filter @sdar/runtime-migration-runner test
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] 并发只一执行
- [ ] 失败保留 DB
- [ ] 版本证据完整

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G2-P2-B06
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G2-P2-B06 --evidence '<command-or-evidence-path>'
```
