# G2-P3-B12 — 完成真实 PM2 集成与 P3 门禁

- **阶段**：P3 PM2 与运行对账
- **依赖**：G2-P3-B07、G2-P3-B08、G2-P3-B10、G2-P3-B11
- **推荐提交**：`test(pm2-adapter): G2-P3-B12 close PM2 runtime phase`

## 目标

使用真实 PM2 启动 baseline Runtime（可用 mock adapter），验证崩溃恢复和健康。

## 允许修改范围

- `tests/pm2-adapter-e2e/**`
- `reports/evidence/**`
- `.codex/reports/goal2-P3.md`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. 启动/ready
2. inspect
3. kill/restart
4. stop/delete
5. 检查非平台进程不可管理
6. 输出 evidence

## 强制验证

```bash
pnpm test:pm2-adapter-e2e
```
```bash
pnpm build
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] 真实 PM2 E2E 通过或合法外部阻断
- [ ] Fake 单元全部通过
- [ ] P3 报告完整

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G2-P3-B12
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G2-P3-B12 --evidence '<command-or-evidence-path>'
```
