# G2-P2-B01 — 定义 DatabaseProfile 与 SecretRef 模型

- **阶段**：P2 数据库自动准备
- **依赖**：G2-P1-B08
- **推荐提交**：`feat(database-profile): G2-P2-B01 define database profiles`

## 目标

建立 PostgreSQL cluster/database/role/profile，禁止明文密码。

## 允许修改范围

- `packages/pms-domain/**`
- `packages/runtime-deployment/**`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. clusterRef/host/db mode/ssl
2. adminSecretRef/runtimeSecretRef
3. provider-specific name policy
4. validation

## 强制验证

```bash
pnpm test:pms
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] 无 password 字段
- [ ] 名称可预测且避免注入
- [ ] profile 不等于连接字符串

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G2-P2-B01
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G2-P2-B01 --evidence '<command-or-evidence-path>'
```
