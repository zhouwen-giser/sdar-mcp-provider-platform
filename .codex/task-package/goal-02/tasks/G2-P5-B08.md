# G2-P5-B08 — 执行 SDAR 真实/受控 Interop E2E

- **阶段**：P5 Provider 集成与交付
- **依赖**：G2-P4-B07、G2-P5-B01
- **推荐提交**：`test(interop): G2-P5-B08 verify SDAR integration`

## 目标

使用 Registry Snapshot 导入 SDAR，执行至少一个 Provider Tool/Task 闭环；无法访问 SDAR 时保留明确外部门禁。

## 允许修改范围

- `tests/sdar-interop/**`
- `reports/evidence/sdar-interop.json`

除非为编译所需的最小依赖修复，不修改上述范围之外的文件；需要越界时先记录 Decision。

## 执行步骤

1. 导入 registry
2. discover/tool visibility
3. tools/call/task lifecycle
4. 通知/获取结果
5. 不伪造 Certified

## 强制验证

```bash
pnpm test:sdar-interop
```

命令名称与当前仓库不完全一致时，先检查 `package.json` 并选择语义等价的现有命令；不得创建永远成功的空脚本替代验证。

## 完成标准

- [ ] 实际成功或 BLOCKED_EXTERNAL 证据合规
- [ ] 不得把 mock-only 声明 system certified
- [ ] 契约差异记录

## 完成证据

- 测试命令输出、Git diff、taskctl evidence 记录。

## 约束与非目标

- 不得扩大到后续任务；发现相关问题写入 decisions/backlog。

## 状态操作

```bash
python3 .codex/task-package/scripts/taskctl.py start G2-P5-B08
# 完成代码与验证后
python3 .codex/task-package/scripts/taskctl.py pass G2-P5-B08 --evidence '<command-or-evidence-path>'
```
