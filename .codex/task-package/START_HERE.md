# Start Here

1. Read `README.md`.
2. Validate package: `bash scripts/validate_package.sh`.
3. Goal 1: `bash scripts/bootstrap_goal1.sh /absolute/repo/path`.
4. Give Codex `CODEX_MASTER_PROMPT.md`.
5. Goal 2 only after Handoff: `bash scripts/prepare_goal2.sh /absolute/repo/path`.
