# Catalog Contract

正式发现链：Runtime ready → `server/discover` → `tools/list` → validate frozen profile → canonicalize → checksum → immutable CatalogSnapshot。禁止管理员修改 Tool schema。`tools/list` V0.1 必须是完整有界目录，不接受未经协议支持的 nextCursor。
